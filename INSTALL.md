# img-api-telegram

```
sudo apt update && sudo apt upgrade
sudo apt install software-properties-common -y

sudo add-apt-repository ppa:deadsnakes/ppa -y
sudo apt update
sudo apt install python3.10 -y

sudo apt install python3-apt python3.10-dev python3.10-venv python3.10-distutils python3.10-lib2to3 python3.10-gdbm python3.10-tk -y

sudo apt-get install python3-pip
```

```
sudo update-alternatives --install /usr/local/bin/python python /usr/bin/python3.10 2
sudo update-alternatives --config python3
```
